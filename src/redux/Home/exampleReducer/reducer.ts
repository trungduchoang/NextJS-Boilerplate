import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const rootContainerReducer = createSlice({
  name: "Home_exampleReducer",
  initialState,
  reducers: {},
  extraReducers: () => {},
});

export default rootContainerReducer.reducer;
